# Optional — keep your Admin API token on the server instead of typing it in the modal.
# If set, the deploy endpoint uses this when the request omits a token.
SHOPIFY_ADMIN_TOKEN=

# Shopify Admin API version used by the proxy.
SHOPIFY_API_VERSION=2026-04

# PORT is provided automatically by Render. Locally it defaults to 3000.
